<h1 class="static-title">Almost ready!</h1>
<div class="container">
  <%= form_for(current_user, as: current_user, url: static_next_path(current_user), html: { method: :patch, class: 'floating-f white-bg mx-auto my-auto shadowed-box w-50' }) do |f| %>
    <p class="text-center my-4 font-italic">One more step, what about an avatar, cover pics and a bio?</p>
    <div class="form-group">
      <div class="d-flex align-items-center justify-content-around">
        <div class="d-block">
          <%= gravatar_circled_for(current_user, 100) %>
        </div>
        <div class="d-flex flex-column">
          <%= f.label :avatar, class: "text-left ml-4" %>
          <%= f.file_field :avatar, direct_upload: true %>
        </div>
      </div>
    </div>
    <div class="form-group">
      <p class="text-center my-4 font-italic">By default a random cover picture is placed, still better be you right?</p>
      <div class="d-flex align-items-center justify-content-around">
        <div class="d-block">
          <img class="img-fluid rounded-lg" src="https://picsum.photos/100?blur=2">
        </div>
        <div class="d-flex flex-column">
          <%= f.label :cover, class: "text-left ml-4" %>
          <%= f.file_field :cover, direct_upload: true do %>
            <label for="file">File to Upload</label> <%= file_field_tag "file" %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="form-group">
      <p class="text-center my-4 font-italic">A nice and short bio would be great for a first impression ain't it?</p>
      <div class="d-flex align-items-center justify-content-around">
        <div class="d-flex flex-column">
          <%= f.label :bio, class: "text-left ml-4" %>
          <%= f.text_field :bio, class: 'form-control' %>
        </div>
      </div>
    </div>
    <div class="form-group d-flex">
      <%= f.submit 'Upload', class: 'btn btn-sm btn-primary' %>
      <div class="ml-auto btn btn-sm outline-primary login"> <%= link_to t('Skip'), root_path, id: 'skip' %></div>
    </div>
  <% end %>
</div>
